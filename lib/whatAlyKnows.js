// One address for the page that will list every fact Aly holds about a
// household, where it came from, and how to export or delete it. Reserved
// September 23, 2026 so the front door, the counsel packet and the trust page
// all point at the same place before the page itself exists.
//
// It stays behind the sign-in, as the finished page must: it will show a
// household's own facts, and there is nothing on it for a stranger.
export const WHAT_ALY_KNOWS_PATH = "/what-aly-knows";
